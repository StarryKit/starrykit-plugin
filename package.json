{
  "name": "starrykit-agent-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "StarryKit Authoring plugin: canonical Skill, host adapters, and Hosted MCP configuration for compatible agent hosts.",
  "scripts": {
    "test": "node --test tests/host-contract.test.mjs",
    "validate": "node scripts/validate-hosts.mjs",
    "validate:root": "python3 scripts/validate.py"
  }
}
